<!DOCTYPE html>

<title>Edit Tag</title>

{% block body %}
    <h1>Edit Tag</h1>

    {{ include('tag_type/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tag_type_index') }}">back to list</a>

    {{ include('tag_type/_delete_form.html.twig') }}
{% endblock %}
