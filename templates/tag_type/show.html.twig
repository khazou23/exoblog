<!DOCTYPE html>

<title>Tag</title>

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ tag.title }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ tag.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tag_type_index') }}">back to list</a>

    <a href="{{ path('tag_type_edit', {'id': tag.id}) }}">edit</a>

    {{ include('tag_type/_delete_form.html.twig') }}
{% endblock %}
